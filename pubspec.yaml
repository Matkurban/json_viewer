name: kurban_json_viewer
description: "A Json To Widget Flutter project.is a JSON string display component that can customize many properties and the style of the display ."
version: 1.0.2
repository: "https://github.com/Matkurban/json_viewer.git"
topics: [ widget ]

environment:
  sdk: ^3.9.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0


